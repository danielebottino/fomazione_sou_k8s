- name: Lab K8s Deploy
  hosts: k8lab
  roles:
    - sou_k8s
  tasks:
    - name: Software Installation
      tags: Softwares
      community.general.homebrew:
        name: "{{ item }}"
        state: present
      loop:
        - podman
        - minikube
        - jenkins
        - kubernetes-cli
